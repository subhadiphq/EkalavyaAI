# ============================================================
# EkalavyaAI — Python Dependencies
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
pydantic>=2.7.4,<3.0.0         # >=2.7.4 required by langsmith on py3.12.4+
pydantic-settings==2.2.1

# ── AI / LLM ─────────────────────────────────────────────────
openai==1.30.0                 # OpenAI-compatible client (OpenRouter, Groq, etc.)
langchain==0.2.1
langchain-openai==0.1.8
langchain-community==0.2.1
langgraph==0.2.0
anthropic==0.28.0              # Direct Anthropic SDK (fallback)
cohere==5.5.0                  # Reranking
voyageai==0.2.3                # voyage-2 embeddings
google-generativeai==0.7.0    # Gemini direct

# ── Vector / Search ─────────────────────────────────────────
pinecone-client==4.1.0
elasticsearch[async]==8.13.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                # PostgreSQL async driver
alembic==1.13.1
redis[asyncio]==5.0.4
neo4j==5.20.0

# ── PDF / Document Generation ────────────────────────────────
weasyprint==61.2
python-docx==1.1.0
PyMuPDF==1.24.3                # PDF parsing

# ── Auth & Security ──────────────────────────────────────────
pyjwt==2.8.0
bcrypt==4.1.3
python-multipart==0.0.9
cryptography==42.0.8
google-auth==2.29.0            # Google OAuth verification

# ── Storage ──────────────────────────────────────────────────
boto3==1.34.113                # AWS S3 / Cloudflare R2

# ── Task Queue ───────────────────────────────────────────────
celery[redis]==5.4.0
flower==2.0.1                  # Celery monitoring

# ── Email ────────────────────────────────────────────────────
resend==1.1.0

# ── Monitoring ───────────────────────────────────────────────
sentry-sdk[fastapi]==2.3.1
langsmith>=0.1.112,<0.2.0     # required by langchain-core 0.2.x

# ── Payment ──────────────────────────────────────────────────
# Cashfree is integrated via its REST API (see services/payment_service.py)
# using httpx, so the cashfree-pg SDK is intentionally not required here.
# The SDK pins sentry-sdk<1.33 which conflicts with sentry-sdk 2.x.

# ── Utilities ────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
python-slugify==8.0.4
pillow==10.3.0                 # Image processing
numpy==1.26.4                  # Numerical operations

# ── Dev / Testing ────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0                  # Test client
factory-boy==3.3.0
